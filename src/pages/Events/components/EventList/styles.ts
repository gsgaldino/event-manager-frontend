import styled from 'styled-components';

export const Wrapper = styled.section`
  display: flex;
  flex-direction: column;
  gap: var(--spacement-x-large);
  padding-block: calc(var(--spacement-large) * 2);
`;
