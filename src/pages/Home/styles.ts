import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: var(--spacement-large);
  align-items: center;
  padding-top: calc(var(--spacement-large) * 5);
  padding-inline:  var(--spacement-default);
  min-height: 70vh;
`;
